# PROD yapılandırması.
apiVersion: v1
kind: ConfigMap
metadata:
  name: fikirsepeti-config
data:
  NODE_ENV: "production"
  EMAIL_PROVIDER: "log_only"
  AZURE_TENANT_ID: "organizations"
  # Azure yönlendirme adresi. NodePort erişimi kullanılırken bu adres Entra
  # uygulama kaydındaki redirect URI ile BİREBİR aynı olmak zorunda; domain
  # bağlanınca ikisi birlikte değiştirilir.
  AZURE_REDIRECT_URI: "http://84.247.180.172:30090/api/auth/azure/callback"
